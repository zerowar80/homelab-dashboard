services:
  homelab-dashboard:
    build: .
    container_name: homelab-dashboard
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
